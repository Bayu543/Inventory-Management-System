@{
    ViewData["Title"] = "Access Denied";
}

<div class="container">
    <div class="row justify-content-center mt-5">
        <div class="col-md-6 text-center">
            <div class="card border-danger">
                <div class="card-body">
                    <i class="fas fa-ban fa-5x text-danger mb-3"></i>
                    <h2 class="text-danger">Akses Ditolak</h2>
                    <p class="lead">Maaf, Anda tidak memiliki izin untuk mengakses halaman ini.</p>
                    <p>Halaman ini hanya dapat diakses oleh pengguna dengan role tertentu.</p>
                    <hr />
                    <a asp-controller="Home" asp-action="Index" class="btn btn-primary">
                        <i class="fas fa-home"></i> Kembali ke Dashboard
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
